{
  "name": "hostel-management",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/build .",
    "start": "cd frontend && npm start"
  }
}
